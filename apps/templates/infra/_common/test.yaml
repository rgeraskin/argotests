apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: test
spec:
  destination:
    name: yc-test-thunder
    namespace: test
    server: ''
  source:
    path: ''
    repoURL: 'registry.gitlab.com/roman-geraskin/test1'
    targetRevision: v0.1.0
    chart: test
  project: default
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:     # Sync options which modifies sync behavior
      - CreateNamespace=true